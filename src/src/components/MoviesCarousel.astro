---
import H3 from "./H3.astro";
import MovieThumbnail from './MovieThumbnail.astro';
const { categoryTitle, projects } = Astro.props;
---
<H3>{categoryTitle}</H3>
{
    projects?.map(project => <MovieThumbnail image={project.data.heroImage} slug={project.slug} />)
}
